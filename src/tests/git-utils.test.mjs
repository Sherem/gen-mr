import { describe, test } from "@jest/globals";
// Legacy placeholder after migration to git-provider. Safe to delete.
describe.skip("legacy git-utils placeholder", () => {
    test("placeholder", () => { });
});
